[package]
name = "forge"
version = "0.1.0"
edition = "2024"

[workspace]
members = [
    "core",
    "viewer",
    "ext-host",
    "protocol",
    "exts/slicer-prusa",
    "drivers/octoprint",
    "drivers/klipper",
]

[dependencies]
forge-viewer = { path = "viewer" }
